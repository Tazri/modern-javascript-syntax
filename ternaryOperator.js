// ternary operator 

// syntax of ternary operator : 

// let variable = condition ? true ? false;

let age = 11;

// let type = age >= 18 ? 'adult' : 'young';
// nested level ternary operator
let type = age >= 18 ? 'adult' : age < 10 ? 'child' : 'young';
console.log(type);

